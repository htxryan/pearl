export const repoUrl = "https://github.com/htxryan/pearl";

// TODO(domain): replace once the .dev domain is registered via Cloudflare Registrar.
// Shortlist + decision criteria + registration steps:
//   docs/runbooks/domain-registration.md
// Used by astro.config.ts (site:, OG image origin) and any page that needs a canonical URL.
export const siteUrl = "https://pearl.example";

export const ogImageUrl = `${siteUrl}/og/site.png`;
