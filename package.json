{
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "pnpm --filter @beads-gui/backend dev",
		"build": "pnpm -r build",
		"test": "pnpm -r test",
		"typecheck": "pnpm -r typecheck"
	},
	"devDependencies": {
		"@playwright/test": "^1.59.1",
		"compound-agent": "^2.7.1"
	}
}
